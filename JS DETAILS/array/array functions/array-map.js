//:: array.map()// method creates a new array populated with the results of calling a provided function on every element in the calling array. condition er upor base kore notun akta array provide korbe. 

var arr = [2,5, 63, 7,3, 74];

var newArrayFromMap = arr.map(value => value * value);
console.log(newArrayFromMap);