var names = ['Rubel', 'Hasan', 'Layek', 'Bhuiya'];

for(const name of names){ //of diye value show kortese
    console.log(name);
}

var names = ['Rubel', 'Hasan', 'Layek', 'Bhuiya'];

for(const name in names){ //in diye index show kortese 
    console.log(name);
}